name: schedule-job

on:
  schedule:
  - cron: "0 0 * * *"
  workflow_dispatch:
  
jobs:
  schedule-job:
    runs-on: ubuntu-latest
    steps:
    - name: code checkout
      uses: actions/checkout@v4
